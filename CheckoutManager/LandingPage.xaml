<Page
    x:Class="CheckoutManager.LandingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CheckoutManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
         
            <TextBlock Style="{ThemeResource TitleTextBlockStyle}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0" Text="Today Is"/>
            <TextBlock Style="{ThemeResource HeaderTextBlockStyle}" HorizontalAlignment="Center" VerticalAlignment="Top"  Text="{x:Bind datestr}" />
            <TextBlock Style="{ThemeResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" Text="Days until next show day:" />
            <TextBlock Style="{ThemeResource HeaderTextBlockStyle}" HorizontalAlignment="Center" Text="{x:Bind dtf}" />
            <TextBlock Style="{ThemeResource SubheaderTextBlockStyle}" HorizontalAlignment="Center" Text="There are currently"/>

        </StackPanel>
    </Grid>
</Page>
